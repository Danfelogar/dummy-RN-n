import { StyleSheet, Text, View } from 'react-native';

import React from 'react';

export const PayInScreen = () => {
  return (
    <View>
      <Text>PayInScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
